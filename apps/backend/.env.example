DATABASE_URL="postgresql://postgres:postgres@localhost:5432/the_desk?schema=public"
PORT=4000
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="replace-with-a-random-secret"
